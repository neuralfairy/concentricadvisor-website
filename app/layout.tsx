import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"
import { Header } from "@/components/header"
import { Footer } from "@/components/footer"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "iEMA - Email Marketing Automation Platform | ConcentricAdvisor Solutions",
  description:
    "Smarter email automation for B2B teams. Increase open rates, optimize timing, and boost conversions with AI-powered email marketing automation.",
  keywords: "email automation, drip campaigns, marketing sequences, iema email marketing, B2B sales tools",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}
